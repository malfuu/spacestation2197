//! Custom Entity Inspector, for observing both client & replicated components.
use bevy::prelude::*;

pub(super) struct ClientInspectorPlugin;

impl Plugin for ClientInspectorPlugin {
    fn build(&self, app: &mut App) {
        // TODO
    }
}
